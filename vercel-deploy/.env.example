# Admin password for the courses management panel
ADMIN_PASSWORD=rithish@admin

# Vercel KV — add via Vercel dashboard > Storage > Create KV Database
# These are set automatically when you connect a KV store
KV_URL=
KV_REST_API_URL=
KV_REST_API_TOKEN=
KV_REST_API_READ_ONLY_TOKEN=

# Vercel Blob — add via Vercel dashboard > Storage > Create Blob Store
# Set automatically when you connect a Blob store
BLOB_READ_WRITE_TOKEN=
